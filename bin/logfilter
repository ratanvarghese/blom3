#!/bin/bash
# vim : set filetype=sh

logsdir=/home/tobias/blog/logs
yesterday=$(blom-tqdate $(date --iso-8601 | tr -d "\n"))
cp $logsdir/access.log $logsdir/dated/$yesterday.log
echo "" > $logsdir/access.log
